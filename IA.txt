Prompt 1: Un refugio de animales quiere reemplazar su mural impreso por un directorio digital interactivo de mascotas en adopcion, exhibido en una pantalla de la recepcion, el equipo del refugio te entrega el listado de mascotas (mascotas.js) y te pide una aplicacion Web SPA en React, simple, ordenada y seguro, que permita ver las mascotas, distinguir su especie y resaltar los casos de adopcion urgente.
Evalucacion: El Resultado fue util,ya que logro hacer un directorio interactivo y ordenada que muestra las mascotas clasificadas por especie y resaltar los casos urgentes, aunque hay un pequeño detalle a mejorar
Modificacion: aun no se modifica nada 

Prompt 2: entrega valores por defecto para evitar errores cuando falte un dato, renderiza el texto siempre como contenido JSX normal, no uses dangerouslySetlnnerHTML y no dejes credenciales, ni claves ni datos sensibles escritos en el codigo
Evalucacion: el Resultado fue util, ya que se agrego los valores por defecto para nombre, raza, especie, descripcion, caracteristicas y adopcion urgente, maneja edad ausente con texto seguro, y evita cualquier use de dangerouslySetlnnerHTMLy no se agrego informacion sensibles ni credenciales
Modificacion: aun nos corrige nada del codigo

promp 3: manteniendo el codigo actual, recorre array con map() y renderiza un mascotaCard por cada mascota, asigna correctamente la prop key usando el id de cada elemento (no uses el indice del array)
Evaluacion:el Resultado fue util, ya que ahora ListaMascotas.jsx renderiza con mascotas.map y key=(mascota.id) y no se utiliza el indice del array.
Modificacion: aun no se modifica nada del codigo

Prompt 4: en app.jsx usa useState para guardar el filtro seleccionado y un campo de busqueda por nombre, aplica ambos filtros sobre el array antes de pasarlo a ListaMascotas, usa renderizado condicional para mostrar el mensaje "no hay mascotas que coincidan" cuando el resultado que vacio
Evaluacion: el resultado fue util ya que el proyecto compila correctamente con la búsqueda por nombre y el filtro aplicado antes del renderizado de ListaMascotas.
Modificacion: aun no se modifica el codigo